{erl_opts, [debug_info]}.
{deps, [
    {jsx, "3.1.0"}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [berl_websocket]}
]}.

{profiles, [
    {test, [
        {deps, [
            {eunit_formatters, "0.5.0"}
        ]},
        {erl_opts, [debug_info, export_all]}
    ]}
]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.

% Plugin configuration
{plugins, []}.

% Release configuration
{relx, [{release, {berl_websocket, "1.0.0"},
         [berl_websocket,
          sasl]},

        {sys_config, "./config/sys.config"},
        {vm_args, "./config/vm.args"},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.
